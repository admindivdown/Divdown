{
  "name": "divdown-backend",
  "version": "1.0.0",
  "main": "server.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server.js",
    "postinstall": "apt-get update && apt-get install -y python3 python3-pip && pip3 install yt-dlp"
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5"
  }
}
